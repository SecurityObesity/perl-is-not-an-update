#!/bin/bash
sudo perl -MCPAN -e 'my $c = "CPAN::HandleConfig"; $c->load(doit => 1, autoconfig => 1); $c->edit(prerequisites_policy => "follow"); $c->commit; $c->edit(build_requires_install_policy => "yes"); $c->commit; $c->edit(show_unparsable_versions => "yes"); $c->commit;'